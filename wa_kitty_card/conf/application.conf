# https://www.playframework.com/documentation/latest/Configuration

play.http.errorHandler = "error.ErrorHandler"

# Increase Pekko coordinated shutdown timeout to prevent premature shutdowns
pekko {
  coordinated-shutdown {
    # Increase the timeout for the coordinated shutdown
    default-phase-timeout = 30s
    
    # Timeout for waiting for ongoing requests to complete
    phases {
      service-requests-done {
        timeout = 30s
      }
      service-stop {
        timeout = 30s
      }
    }
  }
  
  # Actor system configuration
  actor {
    default-dispatcher {
      fork-join-executor {
        parallelism-min = 8
        parallelism-factor = 3.0
        parallelism-max = 64
      }
    }
  }
}

play.filters.hosts {
  allowed = ["localhost", "127.0.0.1", ".trycloudflare.com", "syndication-such-carnival-extreme.trycloudflare.com"]
}

play.filters.disabled += "play.filters.csrf.CSRFFilter" #this needs to be disabled for names, but there for grid idk why